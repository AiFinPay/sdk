# AGENTS.md

> **Shared knowledge base — read before non-trivial or cross-repo work.**
>
> Team source of truth is the vault `github.com/enot3615/knowledge-vault`
> (Obsidian + git; clone it as a sibling `../Obsidian`). Before architectural
> decisions, "we don't have X" claims, or touching a domain you don't own:
>
> - `OWNERS.md` — who owns which repo/domain + the boundary rule (touch a domain
>   you don't own → note in `proposals/` + ping the owner; never silent edits).
> - `00 - Index.md` / `15 - Active Sprint.md` — map + shipped-vs-pending.
> - `AGENTS.md` (in the vault) — how to read/write it. **Never commit secrets.**
