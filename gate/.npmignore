# Defense-in-depth alongside the `files` allowlist in package.json
# (dist, README.md, LICENSE). Never exclude those here.
src/
tests/
tsconfig.json
vitest.config.ts
.gitignore
.mcp.json
coverage/
*.tsbuildinfo
node_modules/
